{
  "name": "cli-config-templates",
  "version": "0.1.0",
  "private": true,
  "description": "Static CLI configuration templates and offline JSON Schema validation tests",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv": "^8.20.0",
    "smol-toml": "^1.8.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11",
    "yaml": "^2.9.0"
  }
}
